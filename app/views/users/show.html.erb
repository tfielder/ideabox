<h1>Logged in as, <%= current_user.username %></h1>
<%= link_to "Log Out", logout_path %>

<%= @ideas.each do |idear| %>
<ul>
  <li>idear.title</li>
  <li>idear.image</li>
  <li>idear.note</li>
</ul>
<% end %>